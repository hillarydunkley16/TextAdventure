import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;
import java.util.*;

class Main {
  public static void main(String[] args) throws Exception {
   Adventure test = new Adventure();
    test.World();
   //  test.constructParlor();
   //  test.constructLibrary();
   //  test.constructMaster_bedroom();
   //  test.constructKitchen();
   
    

  //   File file = new File("map.txt");
  //   Scanner sc = new Scanner(file);
  //   while (sc.hasNextLine()){
  //     System.out.println(sc.nextLine());
    
  // }

  //   // Decision tree test 
  // DecisionTree node = new DecisionTree("Walking");
  // node.setLeft(new DecisionTree("You walked left and entered the parlor"));
  // node.setRight(new DecisionTree("You walked to the right and entered the dining room area"));
  // node.getLeft().setLeft(new DecisionTree("bump into a table"));
  // node.getRight().setRight(new DecisionTree("no one is in the dining room area, you begin to wonder where in the house the party is being held"));
  // node.getLeft().setRight(new DecisionTree("you find a skeleton sitting in the chair. Wow, Jason went all out with the decorations."));
  // node.getRight().setLeft(new DecisionTree("You find wine but no beer"));
  // node.getLeft().getLeft().setLeft(new DecisionTree("you find a picture of a woman that resembles you idk"));
  //   node.getLeft().getLeft().getLeft().setLeft(new DecisionTree(""));
  // node.getLeft().getRight().setRight(new DecisionTree("You look around the room and notice the other decorations"));
  // System.out.println(node.followPath("LLL", node));

    // System.out.println("Julie enters the house and walks into the parlor room");
   
  }
}